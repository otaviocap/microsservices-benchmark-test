# Benchmark Microsservices

Hi, this is a simple benchmark for comparing Go and Rust performance while using a microsservice
architecture


## Steps
* Run `docker compose up`;
* Run `./scripts/run-sql.sh`;
s